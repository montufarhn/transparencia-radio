<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/>
    <stroke
        android:width="15dp"
        android:color="@color/color_light_primary"/> <!-- Color del borde -->
    <corners android:radius="60dp"/> <!-- Radio de las esquinas debe coincidir con el botón circular -->
</shape>
